rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Em produção, restringir por auth. No emulator, permitir tudo.
      allow read, write: if request.auth != null;
    }
  }
}
