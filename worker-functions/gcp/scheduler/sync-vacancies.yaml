# Cloud Scheduler job: sync ClickUp vacancies every 10 minutes
# Service account: configured via GCP deployment pipeline (OQ5 open)
# Requires: Cloud Run service running worker-functions exposing POST /api/internal/sync-clickup-vacancies

name: sync-vacancies-clickup
description: Import vacancies from ClickUp every 10 minutes
schedule: "*/10 * * * *"
timeZone: America/Argentina/Buenos_Aires
httpTarget:
  uri: ${WORKER_FUNCTIONS_SERVICE_URL}/api/internal/sync-clickup-vacancies
  httpMethod: POST
  headers:
    Content-Type: application/json
    X-Internal-Token: ${INTERNAL_SYNC_TOKEN}
  body: e30=  # base64 of '{}'
retryConfig:
  retryCount: 2
  minBackoffDuration: 30s
  maxDoublings: 2
